services:
  ml-pipeline:
    build: .
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    command: >
      sh -c "python src/preprocessing.py && python src/modeling.py && pytest tests/ -q"
